import type { Wallet } from "@saberhq/solana-contrib";
import * as splToken from "@solana/spl-token";
import type { Connection, Keypair, PublicKey, Transaction } from "@solana/web3.js";
export declare const withCreateMint: (transaction: Transaction, connection: Connection, wallet: Wallet, recipient: PublicKey, mintId: PublicKey, amount?: number, freezeAuthority?: PublicKey) => Promise<[PublicKey, Transaction]>;
/**
 * Creates mint, token account, and mints amount to token account
 * @param connection
 * @param creator
 * @returns
 */
export declare const createMint: (connection: Connection, creator: Keypair, recipient: PublicKey, amount?: number, freezeAuthority?: PublicKey) => Promise<[PublicKey, splToken.Token]>;
//# sourceMappingURL=mints.d.ts.map